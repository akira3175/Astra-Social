# CHƯƠNG 3. CÀI ĐẶT – TRIỂN KHAI – KIỂM THỬ

## 3.1 MÔI TRƯỜNG PHÁT TRIỂN & CÔNG NGHỆ ÁP DỤNG
- **Hệ điều hành:** Chạy chéo nền tảng (Windows/macOS/Linux).
- **Môi trường Server Frontend:** Sử dụng Vite Build System (`npm run dev`) cổng mặc định 5173. Node.js >= 18.x.
- **Môi trường Server Backend:** PHP >= 8.2 bằng lệnh `php artisan serve` trên cổng 8000. Web Server (Apache/Nginx tùy chọn).
- **Cơ sở dữ liệu:** MySQL Server (XAMPP/MAMP/Docker tùy điều kiện).
- **Công cụ dòng lệnh:** NPM, Composer, PHP CLI, Git.

## 3.2 HƯỚNG DẪN CÀI ĐẶT & CHẠY DỰ ÁN (LOCAL ENVIRONMENT)

### 1. Chuẩn bị cơ sở dữ liệu (MySQL)
- Sử dụng PHPMyAdmin hoặc MySQL Client tạo một bảng trắng với tên `astra_social`.
- Đảm bảo cơ sở dữ liệu chạy ổn định trên port 3306.

### 2. Thiết lập Backend (Laravel)
- Di chuyển terminal vào thư mục `/backend`.
- **Cài đặt thư viện:** Chạy `composer install`.
- **Thiết lập Môi trường:** Tạo file `.env` bằng cách copy `.env.example`.
- **Sinh khóa bảo mật:** Chạy `php artisan key:generate`.
- **Khai báo Database:** Thiết lập `DB_DATABASE=astra_social` cùng user/password trong file `.env`.
- **Khởi tạo dữ liệu:**
  - Chạy `php artisan migrate` để tạo các bảng SQL tự động.
  - Chạy `php artisan db:seed` nếu cần khởi tạo tài khoản Admin và User gốc.
- **Khởi chạy ứng dụng Server:** `php artisan serve` -> (Listening on http://localhost:8000).

### 3. Thiết lập Frontend (React)
- Di chuyển terminal vào thư mục `/frontend`.
- **Cài đặt thư viện:** Chạy `npm install`.
- **Lưu ý Cấu hình APIURL:** Trong file cấu hình (`axios config`, file `.env`), kiểm tra biến `VITE_API_URL` trỏ về `http://localhost:8000/api`.
- **Khởi chạy Giao Diện:** Chạy `npm run dev` -> (Listening on http://localhost:5173/ hoặc URL tương ứng do Vite quyết định).

**Ghi chú:** Người chấm điểm có thể vào giao diện đăng ký để tạo mới trải nghiệm, hoặc dùng các tài khoản mẫu (nếu đã seed db).

### Tài khoản kiểm thử (Nếu áp dụng seeding):
- **Admin:** (Email: admin@example.com / Pass: password) - Truy cập `/admin`.
- **User Mẫu:** (Email: test@example.com / Pass: password) - Truy cập các tính năng cốt lõi.

---

## 3.3 KỊCH BẢN KIỂM THỬ (TEST CASES)

| TC | Chức năng | Bước thực hiện | Dữ liệu | KQ mong đợi | KQ thực tế |
|---|---|---|---|---|---|
| TC-01 | Đăng ký | Điền đầy đủ Form đăng ký hợp lệ và nhấn Đăng ký. | `Họ Tên`, `Email chưa tồn tại`, `Mật khẩu >= 8 ký tự` | Chuyển hướng sang trang Đăng nhập, thông báo "Đăng ký thành công". | Thông báo tạo tài khoản thành công, DB thêm bản ghi. |
| TC-02 | Đăng ký | Điền Email đã tồn tại trong hệ thống. | `Email đã dùng`, `Mật khẩu` | Hệ thống báo lỗi "Email đã được sử dụng", chữ đỏ viền form. | Báo lỗi Frontend không gọi API thừa. |
| TC-03 | Đăng nhập | Nhập sai mật khẩu của tài khoản. | `Email đúng`, `Sai mật khẩu` | Backend trả về lỗi 401, Hiển thị thông báo "Sai thông tin đăng nhập". | Lỗi hiển thị chính xác, form không bị reset. |
| TC-04 | Đăng nhập | Đăng nhập bằng tài khoản hợp lệ. | `Email đúng`, `Mật khẩu đúng` | Sinh ra token lưu vào localStorage. Chuyển hướng thẳng sang trang Home/Feed. | Vào trang chủ thành công, load Feed ngay. |
| TC-05 | Đăng xuất | Nhấn nút Đăng xuất trên thanh điều hướng. | Không gian giao diện. | Chuyển trang về Login, xóa Token hiện hành, không quay lại bằng nút Back được. | Xóa sạch session/token, bắt đăng nhập lại. |
| TC-06 | Phân quyền (Auth Guard) | Truy cập trực tiếp URL trang chủ mà không có token. | Gõ `/` trên thanh địa chỉ HTTP. | Hệ thống phát hiện không có JWT Token, tự đẩy về trang Login. | Chặn truy cập thành công, trả về Login. |
| TC-07 | Tạo bài viết text | Nhấn vào khung đăng bài, viết text dài và nhấn Đăng. | `"Hôm nay thời tiết đẹp quá mọi người ơi!"` | Dấu vòng xoay "Đang đăng", bài viết hiển thị tức thì lên đầu bảng tin. | Bài mới lên ngay lập tức, lưu SQL kịp thời. |
| TC-08 | Tải ảnh lên | Gắn tệp ảnh vào form Đăng bài nhưng quá dung lượng. | Tệp `.jpg` nặng > 15MB. | Hệ thống báo lỗi dung lượng tệp vượt mức giới hạn. | Không upload được, báo lỗi màu đỏ tại Form. |
| TC-09 | Đăng bài có Hashtags | Nhập nội dung kèm hastag trực tiếp. | `Test #AstraSocial #KiemThu` | Hệ thống tự trích xuất `#AstraSocial`, `#KiemThu` và tô màu xanh clickable trên Feed. | Hiện màu xanh, click vô sẽ sang trang Search. |
| TC-10 | Thả tim (Like) | Click icon trái tim tại bất kỳ bài nào. | ID Bài viết (postId). | Trái tim đỏ lên ngay, Lượt like +1. Nhấn lần hai thì hạ xuống. | Trạng thái hiển thị tức thì (Optimistic UI), mượt mà. |
| TC-11 | Bình luận | Gõ văn bản vào khung bình luận nhấn Gửi. | Dòng văn bản `"Chào anh em"`. | Bình luận nhảy xuống list bên dưới, Count Comment + 1. Nhận chuông báo ngầm. | Comment nhảy đúng dòng, người kia nhận Noti. |
| TC-12 | Cuộn vô hạn (Home Feed) | Kéo chuột xuống cuối trang liên tiếp nhiều lần. | Hành động lướt xuống đáy viewport. | API gọi liên tục offset mỗi 10 bài, không có hiện tượng nạp lại list từ đầu. | Tải mượt, có spinner xoay, kết xuất component nhanh. |
| TC-13 | Kết bạn (Gửi lời mời) | Truy cập trang cá nhân một user và bấm Thêm bạn. | `receiver_id` | Nút "Gửi kết bạn" đổi thành "Chờ phản hồi". Notification bắn sang. | Trạng thái nút bấm cập nhật khớp cơ sở dữ liệu. |
| TC-14 | Chấp nhận kết bạn | Nhấn tab Bạn Bè, chọn "Đồng ý". | List Friendship Request. | Nút biến mất, hai người chính thức hiện trong danh bạ liên lạc bên phải cột. | Chấp nhận thành công, sinh Notification báo lại. |
| TC-15 | Tìm kiếm tổng hợp | Nhập khóa trên Navbar ở form tìm kiếm. | Dữ liệu `Bùi` hoặc `#Test`. | Chuyển trang qua /search, load dsách người dũng tên Bùi và bài đăng khớp text. | Kết quả Search API chính xác, có debounce text. |
| TC-16 | Chat 1-1 | Mở khung Messages, ấn vào tên một bạn bè, gửi tin. | Box Input chat dòng: `"Test tin nhắn"`. | Khung kéo xuống cuối đoạn hội thoại. Tin nhắn mọc ra ở bên phải giao diện mình. | Tin nhắn hiển thị thành công, socket/call mượt. |
| TC-17 | Chat gửi Image | Gửi hình ảnh vào cuộc trò chuyện 1-1/ Nhóm. | URL file Object hoặc chọn trực tiếp file. | Khung chờ, hình ảnh upload thành công hiện thumbnail sắc nét trong tin nhắn. | Upload Cloudinary nhanh, ảnh preview hiển thị đủ. |
| TC-18 | Trending Hashtags | Tạo 3 bài có text giống hashtag A liền nhau. | Post 1..3 cùng nội dung có mã `#HotTrend`. | Sidebar phải của trang Home nhỉnh Ranking `#HotTrend` lên top 1 trong 5p sau nạp. | Redis Ranking cập nhật đúng đắn, sort desc hoàn hảo. |
| TC-19 | Đổi Avatar | Thay đổi ảnh cá nhân. | Bức ảnh vuông PNG kích cỡ vừa phải. | Avartar đổi trên navbar, trong profile, cập nhật ảnh nhỏ trên các comment cũ của mình. | Cập nhật đồng bộ mọi nơi (do lấy liên kết DB user). |
| TC-20 | Admin Khóa tài khoản | Truy cập Admin Dashboard bấm chặn ID của 1 User. | Nút khóa. | Lần sau user đó cố gọi API sẽ nhận 401 hoặc mã báo Blocked. Rút quyền đăng nhập. | Khóa thành công, màn người dùng văng sang trang cấm. |
